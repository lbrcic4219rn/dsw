export const environment = {
  production: true,
  // Point this at the deployed backend. Kept identical to the dev value for now
  // because this file previously had no BASE_URL at all, which broke prod builds.
  BASE_URL: 'http://localhost:8081'
};
